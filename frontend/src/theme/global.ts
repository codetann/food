import { ThemeConfig } from "@chakra-ui/react";

const styles = {
  global: {
    body: {
      _dark: {
        bg: "base.bg.dark",
      },
      _light: {
        bg: "base.bg.light",
      },
    },
  },
};

export { styles };
