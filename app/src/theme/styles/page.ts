import { StyleConfig } from "@chakra-ui/theme-tools";

const base_page: StyleConfig = {
  baseStyle: {
    width: "100vw",
    minHeight: "100vh",
    justifyContent: "center",
    alignItems: "center",
  },
  variants: {},
  sizes: {},
  defaultProps: {},
};

export { base_page };
